<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Blue background -->
    <corners android:radius="32dp" />
    <stroke android:width="2dp" android:color="@color/dashboard_expandable_app_icon" />
</shape>